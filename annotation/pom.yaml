modelVersion: 4.0.0
parent:
  groupId: com.allsimon.quickcheck
  relativePath: ../pom.yaml
  artifactId: parent
  version: '${revision}'

artifactId: annotation

dependencies:
  - {groupId: com.pholser, artifactId: junit-quickcheck-generators}
  - {groupId: io.leangen.geantyref, artifactId: geantyref}
  - {groupId: com.google.auto.service, artifactId: auto-service, scope: provided}
