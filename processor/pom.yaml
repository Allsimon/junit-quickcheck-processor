modelVersion: 4.0.0
parent:
  groupId: com.gitlab.allsimon.quickcheck
  relativePath: ../pom.yaml
  artifactId: parent
  version: 0.0.1-SNAPSHOT

artifactId: processor

dependencies:
  - {groupId: com.gitlab.allsimon.quickcheck, artifactId: annotation, version: 0.0.1-SNAPSHOT}
  - {groupId: com.google.auto.service, artifactId: auto-service}
  - {groupId: com.squareup, artifactId: javapoet}
  - {groupId: com.pholser, artifactId: junit-quickcheck-generators, optional: true}
