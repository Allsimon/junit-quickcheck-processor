modelVersion: 4.0.0
parent:
  groupId: com.gitlab.allsimon.quickcheck
  relativePath: ../pom.yaml
  artifactId: parent
  version: 0.0.1

artifactId: sample

dependencies:
  - {groupId: com.gitlab.allsimon.quickcheck, artifactId: processor, version: 0.0.1, scope: test}
  - {groupId: com.pholser, artifactId: junit-quickcheck-generators, scope: test}
